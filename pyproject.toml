[build-system]
requires = ["setuptools >= 30.3.0"]  # version supporting setup.cfg]
build-backend = "setuptools.build_meta"
[tool.black]
line-length = 88
target-version = ['py39']
include = '\.pyi?$'
exclude = '''
(
  /(
    | .+/migrations
  )/
)
'''
